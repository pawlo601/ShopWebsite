@model dynamic

@{
    ViewBag.Title = "Discounts";
}

<a href="/Home/Index">BACK</a>
<h2>Discounts</h2>

@Model.Count
@foreach (var item in Model)
{
    <div>@item.Id @item.Discount.Name @item.Discount.StartDiscount @item.ProductId</div>
}