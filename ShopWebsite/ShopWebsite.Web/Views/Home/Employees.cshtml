@model dynamic

@{
    ViewBag.Title = "Employees";
}

<a href="/Home/Index">BACK</a>
<h2>Employees</h2>

@Model.Count
@foreach (var item in Model)
{
    <div>@item.Id @item.Email @item.Information.Name @item.Information.Surname @item.Information.Birthday</div>
}

